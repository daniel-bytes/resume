import os
env=Environment(CPPPATH='.',
                CPPDEFINES=[],
                LIBS=[],
                CXXFLAGS="-std=c++2a"
                )
                
files = Glob('*.cpp')
program = env.Program("build/SimpleServ", files)
env.NoClean(program)